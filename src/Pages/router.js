import React from 'react';
import { Route, Switch } from 'react-router-dom';

const Router = () => {
	return (
		<>
			<Router>
				<Switch>
					<Route path="/"></Route>
				</Switch>
			</Router>
		</>
	);
};

export default Router;
