import React from 'react';
// import Stunting from '../Stunting/Stunting';

const HighlightedProduct = ({ children }) => {
	return (
		<>
			<div>{children}</div>
		</>
	);
};

export default HighlightedProduct;
